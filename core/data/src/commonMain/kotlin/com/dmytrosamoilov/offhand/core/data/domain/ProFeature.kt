package com.dmytrosamoilov.offhand.core.data.domain

// What the user was doing when the paywall opened; the paywall leads with it.
enum class ProFeature {
    GENERAL,
    CUSTOM_STYLES,
    DOCUMENT_EXPORT,
    SMART_SUGGESTIONS,
    AUDIO_IMPORT,
}
