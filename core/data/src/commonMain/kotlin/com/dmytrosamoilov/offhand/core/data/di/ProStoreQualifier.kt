package com.dmytrosamoilov.offhand.core.data.di

// The store the platform talks to (Play, StoreKit, or the smoke-test fake);
// the unqualified ProStore wraps it with the debug override.
const val PLATFORM_PRO_STORE = "platformProStore"
